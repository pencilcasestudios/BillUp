<% flash.each do |name, msg| %>
	<% if msg.is_a?(String) %>
	  <%= content_tag :div, class:  "alert-message #{name} fade in out", "data-alert" => "alert" do %>
			<a class="close" href="#">&times;</a>
			<%= msg %>
		<% end %>
	<% end %>
<% end %>
