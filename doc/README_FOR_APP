# Rake for testing
gem cleanup
bundle update
rake db:drop
rake db:setup
bundle exec rake

# Guard for testing
gem cleanup
bundle update
rm -rf public/images/logo
rake db:drop
rake db:setup
bundle exec guard
