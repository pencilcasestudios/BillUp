require 'spec_helper'

describe Membership do
  describe "presence" do
    #it "fails validation with no user_id" do
    #  membership = Membership.new
    #  membership.should have(1).error_on(:user_id)
    #  membership.errors[:user_id].should == ["can't be blank"]
    #end

    #it "fails validation with no organisation_id" do
    #  membership = Membership.new
    #  membership.should have(1).error_on(:organisation_id)
    #  membership.errors[:organisation_id].should == ["can't be blank"]
    #end
  end
end
